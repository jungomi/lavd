import { css } from "emotion";

export const spinner = css({
  display: "flex",
  marginLeft: "1.2rem",
});

export const failed = css({
  display: "flex",
  color: "#888",
  fontStyle: "italic",
  padding: "0.6rem",
});
